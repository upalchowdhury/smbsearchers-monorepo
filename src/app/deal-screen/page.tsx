import { AppPlaceholderPage } from "@/components/layout/AppPlaceholderPage";

export default function DealScreenPage() {
  return (
    <AppPlaceholderPage
      title="DealScreen"
      description="Run structured first-pass quality checks on listings before deeper diligence."
    />
  );
}
