import { AppPlaceholderPage } from "@/components/layout/AppPlaceholderPage";

export default function SettingsPage() {
  return (
    <AppPlaceholderPage
      title="Settings"
      description="Configure account, notifications, source preferences, and workspace defaults."
    />
  );
}
