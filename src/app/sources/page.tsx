import { AppPlaceholderPage } from "@/components/layout/AppPlaceholderPage";

export default function DataSourcesPage() {
  return (
    <AppPlaceholderPage
      title="Data Sources"
      description="Manage source coverage, scrape cadence, and connector health across broker marketplaces."
    />
  );
}
